{
  "name": "enkidu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agentic software engineering harness: worktrees + gates + context + governance.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "lint": "echo \"TODO: configure lint\" && exit 0",
    "test": "echo \"TODO: configure tests\" && exit 0"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "execa": "^9.0.0",
    "pino": "^9.0.0",
    "simple-git": "^3.22.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0"
  }
}
